# Student-Result-Prediction
A Python machine learning project that predicts whether a student will pass or fail using Logistic Regression. The model is trained on attendance and marks data and outputs prediction accuracy and pass probability.
